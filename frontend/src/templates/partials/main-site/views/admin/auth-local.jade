h4
  | Local Users
p.
  Local users are stored in Warehaus' database.
p.
  This works great for small deployments and for creating bot users. For regular
  users in large deployments you may want to consider connecting to an LDAP server
  or use Google login.
p
  | Currently there {{ (users.count == 1) ? 'is' : 'are' }}
  | &#32;
  mild
    | {{ users.count }}
  | &#32;
  | {{ (users.count == 1) ? 'user' : 'users' }} in this backend.

p
  button.btn.btn-primary(type='button' ng-click='createNewUser()')
    | Create a New User
