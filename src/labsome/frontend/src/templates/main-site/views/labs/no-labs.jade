mixin no_labs
  h3.text-center(ng-if='curUser.is_admin')
    i.icon-ghost.x6
  h3.text-center(ng-if='!curUser.is_admin')
    i.icon-clock.x6

  p.text-center.x2
    | No labs were added yet

  p.text-center.x12(ng-if='curUser.is_admin')
    | To add servers and other types of hardware, start by creating a lab.
  p.text-center.x2(ng-if='curUser.is_admin')
    button.btn.btn-lg.btn-success(ui-sref='admin.create-lab')
      | Create a New Lab

  p.text-center.x12(ng-if='!curUser.is_admin')
    | Your administrator is now creating your first lab, it will show up as soon as it's ready

.row
  .col-sm-12.col-md-offset-1.col-md-10.col-lg-offset-2.col-lg-8
    .panel.panel-default
      .panel-body
        +no_labs

