extends ../base/base

append vars
  - html_attributes = {'ng-app': 'labsome.site', 'ng-strict-di': ''}

append head
  base(href='/site/')
  title(ng-controller='TitleController' ng-bind="title || 'Labsome'")

append body
  header
    include top-navbar
  div(ui-view='main')
